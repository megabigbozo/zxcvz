local whitelistecheck = loadstring(game:HttpGet("https://pastebin.com/XPtJGM3c", true))()
if whitelistecheck[_G.key] then
loadstring(game:HttpGet("https://raw.githubusercontent.com/megabigbozo/megabigscripthub/main/gitler", true))
Notification.new("success", "Success whitelisted", "Success body message.")
notif:deleteTimeout(5)
else
game:service('Players').LocalPlayer:Kick('buy key')
end

local Notification = loadstring(game:HttpGet("https://raw.githubusercontent.com/Jxereas/UI-Libraries/main/notification_gui_library.lua", true))()
local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "Title of the library", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest"})


local Tab = Window:MakeTab({
	Name = "Main",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local Section = Tab:AddSection({
	Name = "Mainless"
})

Tab:AddSlider({
	Name = "Slider",
	Min = 16,
	Max = 70,
	Default = 16,
	Color = Color3.fromRGB(255,255,255),
	Increment = 1,
	ValueName = "peniss",
	Callback = function(q1)
	game.Players.LocalPlayers.Character.Humanoid.WalkSpeed = q1
	end    
})

Tab:AddButton({
	Name = "fps 69 2 time = 60 fps",
	Callback = function(v2)
      if v2 then	
		setfpscap(69)
		print(v2)
	  else
		setfpscap(60)
		print(v2)
		Notification.new("warning", "Warning Heading", "fps 60")
  	end    
})

Tab:AddSlider({
	Name = "fps slider",
	Min = 30,
	Max = 200,
	Default = 60,
	Color = Color3.fromRGB(255,255,255),
	Increment = 1,
	ValueName = "fps",
	Callback = function(b2)
		setfpscap(b2)
		print(b2)
	end    
})



OrionLib:Init()
